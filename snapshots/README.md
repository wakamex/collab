# Benchmark Snapshots

This folder stores committed benchmark outputs for reproducible comparison over
time.

- Use `python -m arena.cli --preset baseline-6 --markdown-out snapshots/baseline-6-seed42.md`
  to refresh the canonical baseline snapshot.
- Keep `reports/` for temporary local runs only.
